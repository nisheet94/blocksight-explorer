/**
 * Enum representing the animation classes that are applied when a dropdown
 * menu is shown or hidden.
 */
export const enum DropdownAnimation {
  Enter = 'animate-fade-in-down',
  Exit = 'animate-fade-out-up',
  Default = '',
}
