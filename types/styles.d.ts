export type Styles = {
  /**
   * A key-value pair where the key is a descriptive string identifier for the
   * CSS class, and the value is the corresponding CSS class name.
   */
  [key: string]: string;
};
